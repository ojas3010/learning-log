# week0 feynman 
## 07/07/26
- `split()` - if no argument, it auto becomes into strings, so instead of [1,2,3] it becomes ["1","2","3"] cuz we want **string** and not **int**
- `map()` - i will need to learn on this again
